<UserControl x:Class="Tra_Sua.ThuNgan"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tra_Sua"
             mc:Ignorable="d" 
             d:DesignHeight="594" d:DesignWidth="1129">
    <Grid Background="#F4D8B5" Margin="0,0,0,10" >
        <Border Width="322" Height="164" Background="#F4CBA0" Padding="10"
        CornerRadius="10" BorderBrush="Black" BorderThickness="1"
        HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,16,0,0">

            <Grid>
                <!-- Cấu trúc 3 cột: Avatar - Nội dung - Menu -->
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <!-- Cột Avatar -->
                    <ColumnDefinition Width="*"/>
                    <!-- Cột Nội dung -->
                    <ColumnDefinition Width="30"/>
                    <!-- Cột Menu -->
                </Grid.ColumnDefinitions>

                <!-- Ảnh đại diện -->
                <Border Grid.Column="0" Width="50" Height="50" CornerRadius="25"
                Background="White" BorderBrush="Black" BorderThickness="1"
                HorizontalAlignment="Left" VerticalAlignment="Top">
                    <TextBlock Text="👤" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <!-- Nội dung thông tin nhân viên -->
                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock Text="Nguyen Van A" FontSize="18" FontWeight="Bold"/>

                    <TextBlock Text="Mã Nv: nv100" FontSize="14" Margin="0,2,0,0"/>
                    <TextBlock Text="Ngày Sinh: 2/1/2001" FontSize="14" Margin="0,2,0,0" Width="202"/>

                    <TextBlock Text="Thông tin liên hệ:" FontSize="14" FontWeight="Bold" Margin="0,10,0,0"/>

                    <!-- Số điện thoại -->
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📞" FontSize="14"/>
                        <TextBlock Text=" 0339912629" FontSize="14" FontWeight="Bold"/>
                    </StackPanel>

                    <!-- Email -->
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="✉️" FontSize="14"/>
                        <TextBlock Text=" nguyenvana12@gmail.com" FontSize="14" FontWeight="Bold"/>
                    </StackPanel>
                </StackPanel>

                <!-- Menu dấu ba chấm -->
            </Grid>
        </Border>
        <Border Width="322" Height="164" Background="#F4CBA0" Padding="10"
            CornerRadius="10" BorderBrush="Black" BorderThickness="1"
            HorizontalAlignment="Left" VerticalAlignment="Top" Margin="370,16,0,0">

            <Grid>
                <!-- Cấu trúc 3 cột: Avatar - Nội dung - Menu -->
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <!-- Cột Menu -->
                    <!-- Cột Avatar -->
                    <ColumnDefinition Width="*"/>
                    <!-- Cột Nội dung -->
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>

                <!-- Ảnh đại diện -->
                <Border Grid.Column="0" Width="50" Height="50" CornerRadius="25"
                    Background="White" BorderBrush="Black" BorderThickness="1"
                    HorizontalAlignment="Left" VerticalAlignment="Top">
                    <TextBlock Text="👤" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <!-- Nội dung thông tin nhân viên -->
                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock Text="Nguyen Van A" FontSize="18" FontWeight="Bold"/>

                    <TextBlock Text="Mã Nv: nv100" FontSize="14" Margin="0,2,0,0"/>
                    <TextBlock Text="Ngày Sinh: 2/1/2001" FontSize="14" Margin="0,2,0,0"/>

                    <TextBlock Text="Thông tin liên hệ:" FontSize="14" FontWeight="Bold" Margin="0,10,0,0"/>

                    <!-- Số điện thoại -->
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📞" FontSize="14"/>
                        <TextBlock Text=" 0339912629" FontSize="14" FontWeight="Bold"/>
                    </StackPanel>

                    <!-- Email -->
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="✉️" FontSize="14"/>
                        <TextBlock Text=" nguyenvana12@gmail.com" FontSize="14" FontWeight="Bold"/>
                    </StackPanel>
                </StackPanel>

                <!-- Menu dấu ba chấm -->
            </Grid>
        </Border>
        <Border Width="322" Height="164" Background="#F4CBA0" Padding="10"
            CornerRadius="10" BorderBrush="Black" BorderThickness="1"
            HorizontalAlignment="Left" VerticalAlignment="Top" Margin="722,16,0,0">

            <Grid>
                <!-- Cấu trúc 3 cột: Avatar - Nội dung - Menu -->
                <Grid.ColumnDefinitions>
                    <!-- Cột Menu -->
                    <ColumnDefinition Width="60"/>
                    <!-- Cột Avatar -->
                    <ColumnDefinition Width="*"/>
                    <!-- Cột Nội dung -->
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>

                <!-- Ảnh đại diện -->
                <Border Grid.Column="0" Width="50" Height="50" CornerRadius="25"
                    Background="White" BorderBrush="Black" BorderThickness="1"
                    HorizontalAlignment="Left" VerticalAlignment="Top">
                    <TextBlock Text="👤" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <!-- Nội dung thông tin nhân viên -->
                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock Text="Nguyen Van A" FontSize="18" FontWeight="Bold"/>

                    <TextBlock Text="Mã Nv: nv100" FontSize="14" Margin="0,2,0,0"/>
                    <TextBlock Text="Ngày Sinh: 2/1/2001" FontSize="14" Margin="0,2,0,0"/>

                    <TextBlock Text="Thông tin liên hệ:" FontSize="14" FontWeight="Bold" Margin="0,10,0,0"/>

                    <!-- Số điện thoại -->
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📞" FontSize="14"/>
                        <TextBlock Text=" 0339912629" FontSize="14" FontWeight="Bold"/>
                    </StackPanel>

                    <!-- Email -->
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="✉️" FontSize="14"/>
                        <TextBlock Text=" nguyenvana12@gmail.com" FontSize="14" FontWeight="Bold"/>
                    </StackPanel>
                </StackPanel>

                <!-- Menu dấu ba chấm -->
            </Grid>
        </Border>
        <Border Width="322" Height="164" Background="#F4CBA0" Padding="10"
            CornerRadius="10" BorderBrush="Black" BorderThickness="1"
            HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,198,0,0">

            <Grid>
                <!-- Cấu trúc 3 cột: Avatar - Nội dung - Menu -->
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <!-- Cột Menu -->
                    <!-- Cột Avatar -->
                    <ColumnDefinition Width="*"/>
                    <!-- Cột Nội dung -->
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>

                <!-- Ảnh đại diện -->
                <Border Grid.Column="0" Width="50" Height="50" CornerRadius="25"
                    Background="White" BorderBrush="Black" BorderThickness="1"
                    HorizontalAlignment="Left" VerticalAlignment="Top">
                    <TextBlock Text="👤" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <!-- Nội dung thông tin nhân viên -->
                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock Text="Nguyen Van A" FontSize="18" FontWeight="Bold"/>

                    <TextBlock Text="Mã Nv: nv100" FontSize="14" Margin="0,2,0,0"/>
                    <TextBlock Text="Ngày Sinh: 2/1/2001" FontSize="14" Margin="0,2,0,0"/>

                    <TextBlock Text="Thông tin liên hệ:" FontSize="14" FontWeight="Bold" Margin="0,10,0,0"/>

                    <!-- Số điện thoại -->
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📞" FontSize="14"/>
                        <TextBlock Text=" 0339912629" FontSize="14" FontWeight="Bold"/>
                    </StackPanel>

                    <!-- Email -->
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="✉️" FontSize="14"/>
                        <TextBlock Text=" nguyenvana12@gmail.com" FontSize="14" FontWeight="Bold"/>
                    </StackPanel>
                </StackPanel>

                <!-- Menu dấu ba chấm -->
            </Grid>
        </Border>
        <Button Content="⋮" HorizontalAlignment="Left" Margin="1010,134,0,0" VerticalAlignment="Top" Height="32" Width="24" FontWeight="Bold" FontSize="18" Background="#F4CBA0" BorderBrush="#F4CBA0"/>
        <Button Content="⋮" HorizontalAlignment="Left" Margin="652,134,0,0" VerticalAlignment="Top" Height="32" Width="24" FontWeight="Bold" FontSize="18" Background="#F4CBA0" BorderBrush="#F4CBA0"/>
        <Button Content="⋮" HorizontalAlignment="Left" Margin="303,314,0,0" VerticalAlignment="Top" Height="32" Width="24" FontWeight="Bold" FontSize="18" Background="#F4CBA0" BorderBrush="#F4CBA0"/>
        <Button Content="⋮" HorizontalAlignment="Left" Margin="303,134,0,0" VerticalAlignment="Top" Height="32" Width="24" FontWeight="Bold" FontSize="18" Background="#F4CBA0" BorderBrush="#F4CBA0"/>
        <Border Width="322" Height="164" Background="#F4CBA0" Padding="10"
            CornerRadius="10" BorderBrush="Black" BorderThickness="1"
            HorizontalAlignment="Left" VerticalAlignment="Top" Margin="370,200,0,0">

            <Grid>

                <!-- Menu dấu ba chấm -->
                <!-- Cấu trúc 3 cột: Avatar - Nội dung - Menu -->
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <!-- Cột Menu -->
                    <!-- Cột Avatar -->
                    <ColumnDefinition Width="*"/>
                    <!-- Cột Nội dung -->
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>

                <!-- Ảnh đại diện -->
                <Border Grid.Column="0" Width="50" Height="50" CornerRadius="25"
                    Background="White" BorderBrush="Black" BorderThickness="1"
                    HorizontalAlignment="Left" VerticalAlignment="Top">
                    <TextBlock Text="👤" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <!-- Nội dung thông tin nhân viên -->
                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock Text="Nguyen Van A" FontSize="18" FontWeight="Bold"/>

                    <TextBlock Text="Mã Nv: nv100" FontSize="14" Margin="0,2,0,0"/>
                    <TextBlock Text="Ngày Sinh: 2/1/2001" FontSize="14" Margin="0,2,0,0"/>

                    <TextBlock Text="Thông tin liên hệ:" FontSize="14" FontWeight="Bold" Margin="0,10,0,0"/>

                    <!-- Số điện thoại -->
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📞" FontSize="14"/>
                        <TextBlock Text=" 0339912629" FontSize="14" FontWeight="Bold"/>
                    </StackPanel>

                    <!-- Email -->
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="✉️" FontSize="14"/>
                        <TextBlock Text=" nguyenvana12@gmail.com" FontSize="14" FontWeight="Bold"/>
                    </StackPanel>
                </StackPanel>

            </Grid>
        </Border>
        <Button Content="⋮" HorizontalAlignment="Left" Margin="652,314,0,0" VerticalAlignment="Top" Height="32" Width="24" FontWeight="Bold" FontSize="18" Background="#F4CBA0" BorderBrush="#F4CBA0"/>

    </Grid>
</UserControl>